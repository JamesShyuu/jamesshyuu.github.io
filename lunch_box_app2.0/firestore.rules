service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write;
    }
    
    
    
    // match /userId/{memberId}{
    // 	allow read, write
    // }
  }
}